CONFIG_ZMK_KEYBOARD_NAME = "Corne God"

# Increase transmission strength.
CONFIG_BT_CTLR_TX_PWR_PLUS_8 = y

# No WPM widget
CONFIG_ZMK_WIDGET_WPM_STATUS                     = n
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED          = y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE = y

# Default widget
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN      = y
CONFIG_ZMK_LV_FONT_DEFAULT_SMALL_MONTSERRAT_26 = y
CONFIG_LV_FONT_DEFAULT_MONTSERRAT_26           = y

# Sleep
CONFIG_ZMK_SLEEP              = y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT = 3600000

# Display
CONFIG_ZMK_DISPLAY = y

CONFIG_ZMK_DISPLAY                               = n	# bool	Enable support for displays	n
CONFIG_ZMK_DISPLAY_INVERT                        = n	# bool	Invert display colors from black-on-white to white-on-black	n
CONFIG_ZMK_WIDGET_LAYER_STATUS                   = y	# bool	Enable a widget to show the highest, active layer	y
CONFIG_ZMK_WIDGET_BATTERY_STATUS                 = y	# bool	Enable a widget to show battery charge information	y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE = n	# bool	If battery widget is enabled,        show percentage instead of icons	n
CONFIG_ZMK_WIDGET_OUTPUT_STATUS                  = y	# bool	Enable a widget to show the current output (USB/BLE)	y
CONFIG_ZMK_WIDGET_WPM_STATUS                     = n	# bool	Enable a widget to show words per minute	n


# If CONFIG_ZMK_DISPLAY is enabled, exactly zero or one of the following options must be set to y. The first option is used if none are set.


CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN = y	# Use the built-in status screen
# CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM   = y # Use a custom status screen

# If CONFIG_ZMK_DISPLAY is enabled, exactly zero or one of the following options must be set to y. The first option is used if none are set.

# CONFIG_ZMK_DISPLAY_WORK_QUEUE_SYSTEM    = y	# Use the system main thread for UI updates
# CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED = y	# Use a dedicated thread for UI updates


# RGB
CONFIG_ZMK_BACKLIGHT                    = y
CONFIG_ZMK_BACKLIGHT_BRT_STEP           = 20
CONFIG_ZMK_BACKLIGHT_BRT_START          = 40
CONFIG_ZMK_BACKLIGHT_ON_START           = y
CONFIG_ZMK_BACKLIGHT_AUTO_OFF_IDLE      = y
CONFIG_ZMK_BACKLIGHT_AUTO_OFF_USB       = n


# Uncomment the following lines to enable the Corne RGB Underglow
#CONFIG_WS2812_STRIP                    = y

CONFIG_ZMK_RGB_UNDERGLOW               = y      # bool	Enable RGB underglow	n
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER     = y      # bool	Underglow toggling also controls external power	y
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE = y      # bool	Turn off RGB underglow when keyboard goes into idle state	n
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_USB  = y      # bool	Turn off RGB underglow when USB is disconnected	n
CONFIG_ZMK_RGB_UNDERGLOW_HUE_STEP      = 10     # int	Hue step in degrees (0-359) used by RGB actions	10
CONFIG_ZMK_RGB_UNDERGLOW_SAT_STEP      = 10     # int	Saturation step in percent used by RGB actions	10
CONFIG_ZMK_RGB_UNDERGLOW_BRT_STEP      = 10     # int	Brightness step in percent used by RGB actions	10
CONFIG_ZMK_RGB_UNDERGLOW_HUE_START     = 0      # int	Default hue in degrees (0-359)	0
CONFIG_ZMK_RGB_UNDERGLOW_SAT_START     = 100    # int	Default saturation percent (0-100)	100
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START     = 100    # int	Default brightness in percent (0-100)	100
CONFIG_ZMK_RGB_UNDERGLOW_SPD_START     = 3      # int	Default effect speed (1-5)	3
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START     = 1      # int	Default effect index from the effect list (see below)	0
CONFIG_ZMK_RGB_UNDERGLOW_ON_START      = y      # bool	Default on state	y
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MIN       = 0      # int	Minimum brightness in percent (0-100)	0
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX       = 100    # int	Maximum brightness in percent (0-100)	100


# Values for CONFIG_ZMK_RGB_UNDERGLOW_EFF_START:

# | Value |	Effect      |
# |   0   |   Solid color |
# |   1   |   Breathe     |
# |   2   |   Spectrum    |
# |   3   |   Swirl       |